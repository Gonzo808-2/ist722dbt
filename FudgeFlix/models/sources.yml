version: 2
sources:
    - name: conformed
    database: RAW
    schema: conformed

    tables:
        -name: DateDimension
        columns:
          - name: DateKey
          tests:
            - unique
            
